{
  "compilerOptions": {
    /* 
     * TARGET: Modern JavaScript features that Electron's Chromium understands
     * MODULE: CommonJS because Electron's Node.js environment expects it
     */
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020", "DOM"],
    
    /* 
     * PATHS: Where to put compiled JavaScript files
     * outDir = where TypeScript outputs .js files
     * rootDir = where our source .ts files live
     */
    "outDir": "./dist",
    "rootDir": "./src",
    
    /* 
     * TYPE CHECKING: Strict rules to catch bugs early
     * strict = enables all strict type checking options
     */
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    
    /* 
     * MODULE RESOLUTION: How TypeScript finds imported modules
     * node = use Node.js-style module resolution
     */
    "moduleResolution": "node",
    "resolveJsonModule": true,
    
    /* 
     * SOURCE MAPS: Helps debug TypeScript in the browser/electron
     * When you see an error, it shows you the .ts line, not .js line
     */
    "sourceMap": true
  },
  "include": [
    "src/**/*"
  ],
  "exclude": [
    "node_modules",
    "dist",
    "release"
  ]
}
